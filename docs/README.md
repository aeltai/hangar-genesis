# Documents

<https://hangar.cnrancher.com>

- **Genesis**: Use `hangar genesis --rancher=v2.x.x --tui` (interactive) or `--config=config.yaml` (non-interactive). See project root `README.md` and `generate-list-config.example.yaml`.
- **Image list decisions**: See `IMAGE_LIST_DECISIONS.md` for how the genesis image list is built from charts and KDM.
